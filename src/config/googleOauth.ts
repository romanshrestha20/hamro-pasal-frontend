// src/config/googleOAuth.ts
// This file is optional and not used by the app runtime. The app reads
// NEXT_PUBLIC_GOOGLE_CLIENT_ID directly in src/app/layout.tsx.
// Kept here only for clarity or potential reuse.
export const googleOAuthConfig = {
    clientId: process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID,
};